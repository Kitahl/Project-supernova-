# T0 ruleset source-bound admission probe

Purpose: force a normal post-hardening pull-request event on protocol 2.5 without changing canonical state, active generation, scientific evidence, benchmark cursors, or fresh-work status.

Expected exact-head contexts from the frozen GitHub Actions workflow:

- `supernova/static-control`
- `supernova/report-admission`
- `supernova/transition-admission`

For this non-transition hardening probe, all three should report success after the workflow validates the repository and correctly treats report/transition admission as N/A. This file has no scientific or control authority and may be deleted after the source-bound ruleset is qualified.
