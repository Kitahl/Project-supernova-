# RETRO-0 data-gap receipt

**Status:** `NOT_MEASURED` — consumed normalized Foundry trace inputs were not present in this engineering worktree.

RETRO-0 may use consumed development evidence only. Required minimum fields before estimation:

- stable independent problem/family identity;
- ordered attempt/action events;
- authoritative state snapshot or replayable state delta;
- outcome/failure class;
- exact or conservative complete cost per event;
- terminal status and verification receipt;
- for `W_relax`, a qualified contract/planner representation enabling reconstruction of the first sound relaxed-unreachable checkpoint.

Until those inputs are available, `W_fail`, `W_relax`, `r_relax` and the bracket `W_relax <= W_true_post_doom <= W_fail` remain `NOT_MEASURED`. No numerical estimate is fabricated.
