# O-T0 ruleset attestation probe

Harmless, documentation-only T0 probe created from exact accepted main after the machine-readable ruleset-attestation workflow was installed.

Purpose: trigger the accepted-main read-only ruleset attestation and inspect effective main rules, bypass actors, required status-source bindings, deletion and force-push protections. This file carries zero scientific, calibration, runtime, benchmark, protocol, Revision, or fresh-evidence authority.

Do not merge this probe while issue #16 remains open.
