# O-T0 fail-soft ruleset attestation probe

Harmless documentation-only probe from exact accepted main after the fail-soft ruleset-attestation repair.

Purpose: trigger the accepted-main read-only attestation and record effective main rules, ruleset query results, GitHub Actions App metadata, and typed query failures if any. Zero scientific, calibration, runtime, benchmark, fresh-evidence, protocol, or Revision authority.

Do not merge this probe while issue #16 remains open.
