# Successor countable cohort build gate

Do not freeze the replacement protocol-2.5 cohort until all prospective repairs intended to define its control release are accepted on `main`.

Required pre-freeze checks:
- root TCB epoch 5 accepted;
- #195 nonblocking reasoning-attestation semantics accepted;
- deterministic worker stored-report serialization explicitly carried into every native worker prompt/tooling;
- all open FIX_NOW replay/control defects selected for successor control have protected repair evidence;
- Math Foundry and Mastermind substrate identities are chosen before G is frozen; changing either after G requires another zero-credit replacement;
- exact control release passes Candidate Diagnostics and source-bound admission;
- successor G adds exactly control + assignment + liveness;
- all 12 worker/verifier/integrator/consolidation branches are created from exact G;
- liveness windows are frozen pre-outcome;
- exactly 15 native scheduled lanes are enabled only after structural admission.

The engineering build track remains non-authoritative and may proceed in parallel, but no engineering candidate becomes a successor substrate merely because it exists.
