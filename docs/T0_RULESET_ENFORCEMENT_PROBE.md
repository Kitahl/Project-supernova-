# T0 Ruleset Enforcement Probe

Harmless, documentation-only probe created from exact current main solely to verify that GitHub blocks merges when the three required Supernova admission contexts fail.

This branch name is intentionally outside the trusted reconciler's admitted prefixes, so the authoritative reconciler must emit failure for `supernova/static-control`, `supernova/report-admission`, and `supernova/transition-admission`.

Do not merge this PR. It changes no state, runtime, calibration, benchmark, authority, or scientific evidence.
