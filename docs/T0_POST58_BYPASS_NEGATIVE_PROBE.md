# T0 post-#58 bypass negative probe

Harmless documentation-only probe. This branch prefix is intentionally outside the trusted admission allowlist so the three required Supernova contexts must fail. Do not merge. Used only to verify that the temporary owner bypass used for PR #58 is no longer effective.
