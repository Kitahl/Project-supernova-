# SN-CORE-01 — Implementation Plan

## Dependency graph

`H0 exact worlds -> H1 SOUND kernel -> HDS theorem/test correspondence`

Parallel:

- `Foundry source mount -> OCN-0A five real contracts -> RPG/CAR compiler work`
- `consumed trace corpus -> RETRO-0 + CID-0 + typed-event extraction`
- `TCB/liveness schemas -> negative tests -> future rejection-only admission hardening`

## Current smallest executable slice

H0/H1 is implemented entirely inside this hardening branch and has no dependency on the active experiment bus. It is intentionally small Python plus unittest/jsonschema qualification and has no credentials/path for Foundry execution or Tribunal mutation.

## Next code steps

1. Mount the exact Foundry 3.0.1 source archive and verify SHA-256 before extracting.
2. Select the five OCN pilot operators from actual executable paths.
3. Add/finalize OperatorContract schemas only after the pilot exposes real semantics and unknowns.
4. Supply consumed development traces for RETRO/CID; do not manufacture estimates.
5. Extend typed-event extraction against observed report grammars while keeping the active scheduled one-path rule intact.
6. Connect out-of-band deadline monitoring only as rejection-only hardening and freeze it before countable cohort 1 if it becomes authoritative.
7. Keep calibrated/risk-limited stopping out of Core until a declared fixed-sample or anytime-valid risk construction is separately qualified.
