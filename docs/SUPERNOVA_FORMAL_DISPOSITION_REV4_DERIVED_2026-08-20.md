# Project Supernova — Derived Formal Disposition and Revision-5 Queue

**Date:** 2026-08-20  
**Status:** NON-AUTHORITATIVE DERIVED PLANNING MEMO  
**Protocol:** 2.5 FROZEN  
**Specification:** Revision 4 FROZEN

This document records the latest formal/research disposition supplied to the project and converts it into an implementation queue. It does **not** modify `plan/PLAN.json`, protocol 2.5, Revision 4, any frozen countable control, or scientific status. Where this memo conflicts with the frozen Revision-4 authority or an experiment manifest, the frozen authority controls.

## Current runtime position

Live status is always `main:state/CURRENT.json`. At the time this memo was prepared, Gen7 is the first countable protocol-2.5 replay-only calibration cohort, repository protection is source-bound, fresh work is disabled, and the calibration streak is 0 pending a clean cohort.

## Formal-disposition summary

The latest supplied formal audit classifies the Q-number programme as:

- **14 CLOSED**
- **42 CONDITIONALLY CLOSED**
- **8 EMPIRICAL**
- **0 OPEN**
- **6 IMPOSSIBLE AS STATED**

The important planning consequence is that the project should no longer treat the remaining programme as a broad foundational-mathematics search. Remaining formal work is primarily specification, compiler correctness, mechanisation, certificates, and empirical validation.

### Foundational questions currently treated as conditionally closed

- `Q5` — CONDITIONALLY_CLOSED
- `Q13` — CONDITIONALLY_CLOSED
- `Q14` — CONDITIONALLY_CLOSED
- `Q16` — CONDITIONALLY_CLOSED
- `Q27` — CONDITIONALLY_CLOSED
- `Q43` — CONDITIONALLY_CLOSED

This is a research disposition, not a protocol mutation. Any future machine-readable encoding belongs to Revision 5 after the two-clean-cohort freeze opens.

## Residual work classification

| Object | Disposition |
| --- | --- |
| `STATE_COMPILER_0` | FORMAL_IMPLEMENTATION_PROOF |
| `Q14_REVALIDATION` | MECHANISATION |
| common-cause modelling | EMPIRICAL_IDENTIFICATION |
| reaction value | EMPIRICAL |
| selector value | EMPIRICAL |
| ignition value | EMPIRICAL |
| `G8` | PROSPECTIVE_CONFIRMATION |

## Revision-5 stage-order correction — QUEUED, NOT ACTIVE

The frozen Revision-4 machine-readable plan must not be changed during the current calibration freeze. For Revision 5, queue the following causal/implementation ordering before learned selection:

`C1 -> SN-RXN -> DR03 -> E3 -> SELECT -> IGNITION -> G8`

Rationale: verified-product closure must precede runtime reaction records; reaction records must precede DR03 causal semantics; DR03 must precede learned mechanism/selector claims; ignition remains value-gated continuation; G8 is the finite prospective cascade test.

This queue is derived from the current research issue ordering and does not retroactively alter Gen7.

## Revision-5 planning-math rulings — QUEUED

The next specification should make the following explicit:

1. **Decision width**
   - use constrained induced width as the tractability quantity;
   - do not create a new bespoke `tau_dec` theory if the inherited notion suffices.

2. **No separate r-width programme**
   - continuation quotient size may remain a semantic diagnostic;
   - if a complete `s`-variable boundary with domains at most `q` is sufficient, record the bound `r <= q^s` rather than promoting `r` as a new width theory.

3. **Resumable strategy representation**
   - replace compact atomic option libraries with a resumable strategy DAG / finite-state transducer where interleaving matters.

4. **Information fidelity**
   - every compiled decision at time `t` must be measurable with respect to the primitive information filtration `F_t`;
   - no compiled controller may condition on information primitive execution has not observed.

5. **Strategy-Faithful Compilation Certificate**
   - make this a first-class formal certificate:

> Every legal compiled strategy corresponds to a legal primitive strategy and vice versa, preserving observation timing, action information sets, legality, resources, verification, stopping, consumer-visible trajectory law, and objective.

This is the principal remaining planning-side formal proof obligation; target-side optimization machinery should be inherited rather than reinvented where possible.

## H0/H1 exact failure fixtures — QUEUED

Add the following named non-admissible qualification fixtures in the next specification/control revision:

- `PAIR`
- `XOR`
- `ORDER`
- `ENABLE`
- `SEPARATOR`
- `MEMORY`
- `COMMUTE`
- `RESOURCE`
- `HIDDEN_CONTROLLER`
- `INTERLEAVE`

Intended falsifiers include insufficient information state, invalid commutation, lost enablement/action-set changes, missing controller memory, hidden resource coupling/double-spend, oracle leakage, and false atomic-option assumptions.

## Tribunal autonomy — QUEUED CLARIFICATION

Revision 4 already enforces:

`State_Supernova_control INTERSECT State_Tribunal_scientific = EMPTY`

Revision 5 should state the architectural implication explicitly:

> Tribunal is an autonomous scientific research/adjudication system and does not require Supernova to operate. Supernova is an optional finite reaction/cascade amplification and control layer that consumes references to Tribunal-admitted scientific objects without owning Tribunal's scientific state.

## Research-allocation rule

Do not restart a broad foundational Deep Research programme merely because T0 closes. Use targeted prior work only to answer implementation-contract, inherited-machinery, baseline, and empirical-design questions. Reopen a foundational theorem target only when a concrete upstream falsifier or unmet formal hypothesis justifies it.

## Freeze rule

No item in this memo authorizes:

- protocol 2.6;
- Revision 5;
- mutation of the Gen7 frozen control;
- fresh scientific evidence;
- retrospective calibration credit;
- a change to Tribunal scientific state.

The machine-readable Revision-5 changes are queued until two consecutive clean countable protocol-2.5 cohorts complete.